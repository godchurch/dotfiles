$if Bash

set colored-stats On
set colored-completion-prefix On
set expand-tilde On

"\e[Z": menu-complete
"\e\C-n": "virsh -c qemu:///system start live && virt-manager -c qemu:///system --show-domain-console live"

$endif

# vim: ft=readline
